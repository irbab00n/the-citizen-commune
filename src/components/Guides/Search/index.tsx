import React from 'react';

import Row from '../../Layout/Row';

interface GuideSearchProps {
  [key: string]: any;
}

const GuideSearch = (props: GuideSearchProps) => {
  return <Row className="guide-search">Search</Row>;
};

export default GuideSearch;
