import React from 'react';

import Row from '../../Layout/Row';

interface FeaturedArticleProps {
  [key: string]: any;
}

const FeaturedArticle = (props: FeaturedArticleProps) => {
  return <Row>Featured Article</Row>;
};

export default FeaturedArticle;
