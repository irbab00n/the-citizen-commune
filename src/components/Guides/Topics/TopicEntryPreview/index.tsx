import React from 'react';

import Column from '../../../Layout/Column';

interface TopicEntryPreviewProps {
  [key: string]: any;
}

const TopicEntryPreview = (props: TopicEntryPreviewProps) => {
  return <Column size={2}>Topic Entry Preview</Column>;
};

export default TopicEntryPreview;
