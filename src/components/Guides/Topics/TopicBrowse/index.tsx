import React from 'react';

import Column from '../../../Layout/Column';

interface TopicBrowseProps {
  [key: string]: any;
}

const TopicBrowse = (props: TopicBrowseProps) => {
  return <Column>Topic Browse</Column>;
};

export default TopicBrowse;
